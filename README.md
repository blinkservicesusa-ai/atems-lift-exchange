# ATEMS Lift Exchange

This is a Vite + React project for the ATEMS Lift Exchange website.

## Edit equipment listings
Open `src/main.jsx` and edit the `equipment` array near the top of the file.

## Vercel build settings
- Framework Preset: Vite
- Build Command: `npm run build`
- Output Directory: `dist`
